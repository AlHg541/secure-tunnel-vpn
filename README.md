\# Secure Tunnel VPN

Computer Networks Final Project (Phase 1 \& 2) - User-space VPN core with full control plane.



\## Structure

\- `server/` : VPN gateway + Admin/Client web panels (runs on host)

\- `client/` : TUN client (runs in VM)

\- `docs/`   : design notes

\- `tests/`  : test scripts

